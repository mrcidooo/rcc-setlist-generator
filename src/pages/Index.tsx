"use client"; import { useState, type FormEvent } from "react"; import { Music, Users, Calendar, FileText, Upload, UserPlus, Library, Moon, Sun } from "lucide-react"; import { Button } from "@/components/ui/button"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/dialog"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"; import { useToast } from "@/hooks/use-toast"; import { MadeWithDyad } from "@/components/made-with-dyad"; import { useTheme } from "next-themes"; type VoiceType = "male" | "female"; type Singer = { id: string; name: string; nickname: string; voiceType: VoiceType; notes: string; }; type QuickActionType = "upload" | "singer" | "setlist" | "pdf"; type QuickAction = { title: string; icon: React.ComponentType<{ className?: string }>; color: string; action: QuickActionType; }; const dashboardStatsBase = [ { title: "Total Songs", value: "42", icon: Music, color: "text-blue-600" }, { title: "Upcoming Setlists", value: "5", icon: Calendar, color: "text-purple-600" }, { title: "Recently Added", value: "3", icon: FileText, color: "text-orange-600" }, ]; const quickActions: QuickAction[] = [ { title: "Upload Song", icon: Upload, color: "bg-blue-500", action: "upload" }, { title: "Add Singer", icon: UserPlus, color: "bg-green-500", action: "singer" }, { title: "Create Setlist", icon: Library, color: "bg-purple-500", action: "setlist" }, { title: "Generate PDF", icon: FileText, color: "bg-orange-500", action: "pdf" }, ]; const initialSingers: Singer[] = [ { id: "1", name: "John Smith", nickname: "Johnny", voiceType: "male", notes: "Tenor" }, { id: "2", name: "Sarah Johnson", nickname: "Sara", voiceType: "female", notes: "Alto" }, { id: "3", name: "Mike Davis", nickname: "", voiceType: "male", notes: "Bass" }, ]; const getVoiceTypeLabel = (voiceType: VoiceType) => { return voiceType === "male" ? "Male" : "Female"; }; export default function Index() { const [isUploadDialogOpen, setIsUploadDialogOpen] = useState(false); const [isSingerDialogOpen, setIsSingerDialogOpen] = useState(false); const [isSetlistDialogOpen, setIsSetlistDialogOpen] = useState(false); const [singers, setSingers] = useState<Singer[]>(initialSingers); const [singerForm, setSingerForm] = useState({ name: "", nickname: "", voiceType: "male" as VoiceType, notes: "", }); const { toast } = useToast(); const { resolvedTheme, setTheme } = useTheme(); const isDark = resolvedTheme === "dark"; const dashboardStats = [ ...dashboardStatsBase, { title: "Total Singers", value: singers.length.toString(), icon: Users, color: "text-green-600" }, ]; const resetSingerForm = () => { setSingerForm({ name: "", nickname: "", voiceType: "male", notes: "", }); }; const handleQuickAction = (action: QuickActionType) => { switch (action) { case "upload": setIsUploadDialogOpen(true); break; case "singer": setIsSingerDialogOpen(true); break; case "setlist": setIsSetlistDialogOpen(true); break; case "pdf": toast({ title: "PDF generation coming soon", description: "This feature will be available soon.", }); break; } }; const handleAddSinger = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!singerForm.name.trim()) { toast({ title: "Singer not added", description: "Please enter a singer name first.", }); return; } const newSinger: Singer = { id: Date.now().toString(), name: singerForm.name.trim(), nickname: singerForm.nickname.trim(), voiceType: singerForm.voiceType, notes: singerForm.notes.trim(), }; setSingers((currentSingers) => [newSinger, ...currentSingers]); resetSingerForm(); setIsSingerDialogOpen(false); toast({ title: "Singer added", description: `${newSinger.name} has been added to your team.`, }); }; return ( <div className="min-h-screen bg-gray-50 dark:bg-gray-900 pb-20 flex flex-col"> <header className="bg-white dark:bg-gray-800 shadow-sm p-4 flex justify-between items-center"> <div> <h1 className="text-2xl font-bold text-gray-900 dark:text-white">Worship Setlist Generator</h1> <p className="text-sm text-gray-600 dark:text-gray-400">Church Worship Team</p> </div> <Button variant="ghost" size="icon" aria-label="Toggle dark mode" onClick={() => setTheme(isDark ? "light" : "dark")} > <Sun className={`h-5 w-5 ${isDark ? "hidden" : "block"}`} /> <Moon className={`h-5 w-5 ${isDark ? "block" : "hidden"}`} /> </Button> </header> <div className="p-4 flex-1"> <div className="grid grid-cols-2 gap-4 mb-6"> {dashboardStats.map((stat, index) => { const Icon = stat.icon; return ( <Card key={index} className="border-0 shadow-md"> <CardContent className="p-4 text-center"> <Icon className={`h-8 w-8 mx-auto mb-2 ${stat.color}`} /> <div className="text-2xl font-bold">{stat.value}</div> <div className="text-sm text-gray-600 dark:text-gray-400">{stat.title}</div> </CardContent> </Card> ); })} </div> <h2 className="text-lg font-semibold mb-4 text-gray-900 dark:text-white">Quick Actions</h2> <div className="grid grid-cols-2 gap-4 mb-6"> {quickActions.map((action, index) => { const Icon = action.icon; return ( <Button key={index} className={`${action.color} text-white h-20 flex-col gap-2`} onClick={() => handleQuickAction(action.action)} > <Icon className="h-6 w-6" /> <span className="text-sm font-medium">{action.title}</span> </Button> ); })} </div> <h2 className="text-lg font-semibold mb-4 text-gray-900 dark:text-white">Team Members</h2> <div className="space-y-3 mb-6"> {singers.slice(0, 3).map((singer) => ( <Card key={singer.id} className="border-0 shadow-sm"> <CardContent className="p-3"> <div className="font-medium text-gray-900 dark:text-white">{singer.name}</div> <div className="text-sm text-gray-600 dark:text-gray-400"> {getVoiceTypeLabel(singer.voiceType)} {singer.nickname ? ` • ${singer.nickname}` : ""} {singer.notes ? ` • ${singer.notes}` : ""} </div> </CardContent> </Card> ))} </div> <h2 className="text-lg font-semibold mb-4 text-gray-900 dark:text-white">Recent Songs</h2> <div className="space-y-3"> {[1, 2, 3].map((i) => ( <Card key={i} className="border-0 shadow-sm"> <CardContent className="p-3 flex items-center justify-between"> <div> <div className="font-medium">Amazing Grace</div> <div className="text-sm text-gray-600 dark:text-gray-400">Key: C • Added: 2 days ago</div> </div> <Button variant="ghost" size="sm"> <FileText className="h-4 w-4" /> </Button> </CardContent> </Card> ))} </div> </div> <Dialog open={isUploadDialogOpen} onOpenChange={setIsUploadDialogOpen}> <DialogContent> <DialogHeader> <DialogTitle>Upload New Song</DialogTitle> </DialogHeader> <div className="space-y-4"> <div> <Label>Song Title</Label> <Input placeholder="Enter song title" /> </div> <div> <Label>Original Key</Label> <Input placeholder="e.g., C, D, G" /> </div> <div> <Label>Tags</Label> <Input placeholder="e.g., Praise, Worship, Upbeat" /> </div> <div> <Label>PDF File</Label> <Input type="file" accept="application/pdf" /> </div> <Button className="w-full">Upload Song</Button> </div> </DialogContent> </Dialog> <Dialog open={isSingerDialogOpen} onOpenChange={setIsSingerDialogOpen}> <DialogContent> <DialogHeader> <DialogTitle>Add New Singer</DialogTitle> </DialogHeader> <form onSubmit={handleAddSinger} className="space-y-4"> <div> <Label htmlFor="singer-name">Full Name</Label> <Input id="singer-name" value={singerForm.name} onChange={(event) => setSingerForm((current) => ({ ...current, name: event.target.value }))} placeholder="Enter full name" required /> </div> <div> <Label htmlFor="singer-nickname">Nickname (Optional)</Label> <Input id="singer-nickname" value={singerForm.nickname} onChange={(event) => setSingerForm((current) => ({ ...current, nickname: event.target.value }))} placeholder="Enter nickname" /> </div> <div> <Label htmlFor="singer-voice-type">Voice Type</Label> <Select value={singerForm.voiceType} onValueChange={(value) => setSingerForm((current) => ({ ...current, voiceType: value as VoiceType })) } > <SelectTrigger id="singer-voice-type"> <SelectValue placeholder="Select voice type" /> </SelectTrigger> <SelectContent> <SelectItem value="male">Male</SelectItem> <SelectItem value="female">Female</SelectItem> </SelectContent> </Select> </div> <div> <Label htmlFor="singer-notes">Notes (Optional)</Label> <Input id="singer-notes" value={singerForm.notes} onChange={(event) => setSingerForm((current) => ({ ...current, notes: event.target.value }))} placeholder="e.g., Tenor, Alto, Lead" /> </div> <div className="flex justify-end space-x-3"> <Button type="button" variant="ghost" onClick={() => { resetSingerForm(); setIsSingerDialogOpen(false); }} > Cancel </Button> <Button type="submit">Add Singer</Button> </div> </form> </DialogContent> </Dialog> <Dialog open={isSetlistDialogOpen} onOpenChange={setIsSetlistDialogOpen}> <DialogContent className="max-w-md"> <DialogHeader> <DialogTitle>Create New Setlist</DialogTitle> </DialogHeader> <div className="space-y-4"> <div> <Label>Setlist Name</Label> <Input placeholder="e.g., Sunday Service, Christmas Eve" /> </div> <div> <Label>Event Date</Label> <Input type="date" /> </div> <div> <Label>Select Songs</Label> <Select> <SelectTrigger> <SelectValue placeholder="Add songs to setlist" /> </SelectTrigger> <SelectContent> <SelectItem value="1">Amazing Grace - C</SelectItem> <SelectItem value="2">How Great Thou Art - G</SelectItem> <SelectItem value="3">Blessed Assurance - F</SelectItem> </SelectContent> </Select> </div> <Button className="w-full" onClick={() => { setIsSetlistDialogOpen(false); toast({ title: "Setlist Created", description: "Your new setlist has been created successfully.", }); }} > Create Setlist </Button> </div> </DialogContent> </Dialog> <footer className="mt-auto"> <MadeWithDyad /> </footer> </div> ); }